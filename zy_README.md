# ChatSystem - zy的任务
- 数据库表设计与初始化
- 用户认证系统（含验证码）
- 会话管理与心跳检测
- 消息存储与查询接口

## 1. 数据库管理 (`server/database/`)
- **文件**: `db_manager.py`
- **功能**: 
  - 自动初始化 SQLite 数据库，创建 `users` 和 `messages` 表结构。
  - 用户注册登录状态管理模块
  - 消息存储与撤回模块

## 2. 用户认证系统 (`server/user/`)
- **文件**: `user_handler.py`
- **功能**: （暂时未包含验证码）
  - 用户登录，用户名不能重复
  - 登录密码大于等于6位
    
## 3. 会话管理和心跳检测 (`server/user/`)
- **文件**: `session_manager.py`
- **功能**: 
  - 离线状态：如果30s内没发消息也没有回复心跳包就认为离线
  - 判断用户在线/离线状态
  
## 4. 消息存储和查询接口 (`server/message/`)
- **文件**: `history_manager.py`
- **功能**:
  - **历史查询**:查询自定义数量历史消息（默认为50条）；如果某条消息被撤回则替换为 "该消息已撤回"。
  - **消息撤回**：暂时未添加撤回时间限制，可撤回任意时间发出的消息（若需要再加）
